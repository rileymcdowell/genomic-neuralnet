from config.constants import \
        CPU_CORES, CYCLES, REQUIRED_MARKERS_PROPORTION, \
        TRAIN_SIZE, TRAIT_NAME, MAX_EPOCHS, CONTINUE_EPOCHS
from config.data_definitions import \
        markers, pheno 
